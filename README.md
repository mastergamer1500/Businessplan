# BusinessPlan

A Flask-based business planning dashboard with login, setup, dashboard, and AI assistant features.

## Local run

```bash
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python app.py
```

## Deployment

This repository is prepared for Render with a Procfile and backend dependency list.
